// This prints the passwords after they went through the BTOA process so you can enter them in future reference.
console.log(btoa("batch"))
console.log(btoa("coder"))
console.log(btoa("sex"))
console.log(btoa("memes"))